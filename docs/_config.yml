# Jekyll 配置文件 (GitHub Pages)

title: 他山团队 Claude Code 插件集合
description: 为学术研究提供专业的工作流支持

# Markdown 设置 - 使用 GFM 解析器
markdown: kramdown
kramdown:
  input: GFM
  syntax_highlighter_opts:
    disable: true

# 排除的文件
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/
