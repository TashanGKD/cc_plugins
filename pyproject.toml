[project]
name = "cc-plugins-docs"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "mkdocs-material>=9.7.1",
    "mkdocs-git-revision-date-localized-plugin>=1.5.0",
]

[dependency-groups]
dev = []
