# /manim 命令

Manim 一键动画生成命令 - 输入描述，自动输出 1080p 高清视频。

## 前置要求

```bash
# 本地需要安装 Manim
pip install manim

# 验证安装
manim --version
```

## 用法

```
/manim <description>
```

直接用自然语言描述你想要的动画，自动生成 1080p 高清视频。

## 示例

```
/manim 绘制一个正弦函数图像
/manim 创建一个圆变形成正方形的动画
/manim 展示 E=mc^2 公式动画
/manim 三个球体在平面上运动碰撞
/manim 矩阵乘法的可视化
```

## 自动化流程

```
输入描述
    ↓
1. 生成 Manim Python 代码
    ↓
2. 保存为 scene.py
    ↓
3. 自动编译 (1080p)
    ↓
4. 输出视频文件
```

**输出**: `manim_outputs/<scene_name>_<timestamp>/output/high/<SceneName>.mp4`

## 高级用法

### 指定质量

```
/manim 绘制圆 --quality 4k
/manim 正弦函数 --quality low      # 快速预览
```

### 指定格式

```
/manim 圆形动画 --format gif
/manim 函数图像 --format png
```

### 查看工具函数

```
/manim tools circle               # 查看圆形相关工具
/manim tools plot                 # 查看绘图工具
```

### 只生成代码（不编译）

```
/manim 代码：绘制正弦函数 --code-only
```

## 质量选项

| 选项 | 分辨率 | 用途 |
|------|--------|------|
| `low` | 480p | 快速预览（~10秒） |
| `medium` | 720p | 标准输出（~30秒） |
| `high` | 1080p | **默认** 高质量（~1分钟） |
| `4k` | 2160p | 超高清（~5分钟） |

## 完整示例

### 示例 1: 基础使用

```
用户: /manim 绘制一个蓝色正弦函数图像

Claude:
🎬 生成动画: 正弦函数
📝 代码生成中...
✅ 代码已保存: sine_wave_scene.py
🎨 开始编译 (1080p)...

[编译中...]

✅ 渲染完成!
📁 视频文件: manim_outputs/sine_wave_scene_<timestamp>/output/high/SineWaveScene.mp4
⏱️  耗时: 45 秒

💡 预览视频: xdg-open manim_outputs/sine_wave_scene_<timestamp>/output/high/SineWaveScene.mp4
```

### 示例 2: 快速预览

```
用户: /manim 圆形变正方形 --quality low

Claude:
🎬 生成动画: 圆形变形
📝 代码生成中...
✅ 代码已保存: morph_scene.py
🎨 开始编译 (480p 预览)...

[编译中...]

✅ 渲染完成! (快速预览)
📁 视频文件: manim_outputs/morph_scene_<timestamp>/output/low/MorphScene.mp4
⏱️  耗时: 8 秒
```

### 示例 3: 只生成代码

```
用户: /manim 绘制余弦函数 --code-only

Claude:
🎬 生成代码: 余弦函数
✅ 代码已保存: cosine_scene.py

💡 编译命令: python plugins/manim-creator/skills/manim-builder/scripts/build.py cosine_scene.py --quality high
```

## 故障排除

### 错误: manim 未安装

```
❌ 错误: manim 未安装

解决方案:
pip install manim
```

### 错误: 代码有语法错误

```
❌ 编译失败: 语法错误

正在修复代码...
[自动修复中...]

✅ 代码已修复，重新编译...
📁 视频文件: manim_outputs/<scene_name>_<timestamp>/output/high/...
```

### 编译时间过长

```
⚠️  渲染时间较长...

建议:
1. 使用 --quality low 快速预览
2. 简化动画复杂度
3. 减少对象数量
```

## 输出目录

```
manim_outputs/
└── <scene_name>_<timestamp>/
    ├── scene.py                   # 源代码
    ├── config.json                # 生成配置
    ├── output/
    │   └── high/                  # 默认 1080p 输出
    │       └── <SceneName>.mp4
    ├── logs/
    │   └── build.log              # 编译日志
    └── README.md
```
